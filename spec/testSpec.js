// input = [60,10,45,60,1500]
// output = [60,40,45,60,1000]

describe('frequency', () => {
  it("should bring frequency to max number of 1000", () => {
    let input = [60,10,45,60,1500];

    let frequencyWithInput = frequency(input);
    //
    // expect(frequencyWithInput).toEqual([60,40,45,60,1000]);
  })
})
